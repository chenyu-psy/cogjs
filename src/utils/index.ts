const random = require('./random')
const shuffleArray = require('./shuffleArray')
const colorWheel = require('./colorWheel')
const attentionCheck = require('./attentionCheck')
const counterBalance = require('./counterBalance')
const convertCase = require('./convertCase')
const convertColor = require('./convertColor')
const convertTime = require('./convertTime')
const countdownTimer = require('./countdownTimer')
const getIndex = require('./getIndex')
const sequence = require('./sequence')
const calculateAngle = require('./calculateAngle')
const chunkTrials = require('./chunkTrials')    

module.exports = {
    random,
    shuffleArray,
    colorWheel,
    attentionCheck,
    counterBalance,
    convertCase,
    convertColor,
    convertTime,
    countdownTimer,
    getIndex,
    sequence,
    calculateAngle,
    chunkTrials
};